#include <stdio.h>

int  main() {

   int product, max;
   int i, j;
   for (i = 999; i >= 100; i--) {
      for (j = 999; j >= 100; j--) {
      product = i * j;

   printf("%d\n", n);
   return 0;
}
